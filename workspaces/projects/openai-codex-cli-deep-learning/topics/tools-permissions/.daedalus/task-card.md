# 学习任务卡

- 任务：`openai-codex-cli-deep-learning`
- 创建时间：`2026-05-09 15:58:52`
- 学习材料类型：代码仓库
- 当前目标：深入学习 OpenAI Codex CLI 源码，先建立整体架构地图，再重点理解 Agent 主循环、上下文管理、工具调用、沙箱/权限边界，并抽取可复现的 mini demo。
- 现实问题：希望掌握真实 Agent CLI 项目的工程组织与核心执行链路，为后续设计或改造自己的 Agent/CLI 系统提供可迁移模式。
- 预期输出物：可复现运行 runbook、问题路线图、架构笔记、核心代码精读笔记、mini demo 设计与实现、阶段性验证记录。
- 验收标准：能够从 CLI 入口解释一次用户请求如何进入 Agent loop、如何构造上下文、如何选择/执行工具、如何处理权限与沙箱；能够本地运行或明确记录无法运行原因；能够实现一个保留 Codex 核心架构决策的最小 demo。
- 约束条件：外部 Codex 源码放在 `source/` 且默认不提交；每个阅读动作必须服务于 runbook、架构笔记、代码精读或 demo；学习过程中保持 `workspaces/02-learning` 只有一个 active task。
- 暂不学习：暂不做泛泛的 LLM 原理学习；暂不追求覆盖所有边缘命令、所有平台适配和完整 UI 细节；暂不把 Codex 当作黑盒使用教程。
- 用户确认：用户已确认学习对象为 OpenAI Codex CLI，学习重点为 Agent loop、沙箱/权限和整体架构，输出物为 runbook、架构笔记、核心代码精读和 mini demo。

## Repo 学习目标

- 希望获得的能力：读懂生产级 Agent CLI 的核心执行链路、边界设计、权限模型和可复用实现模式。
- 候选 repo 方向：OpenAI Codex CLI / `openai/codex`。
- Repo 选择约束：优先选择官方仓库；必须可本地拉取；优先固定 commit 或 tag，保证后续笔记可复现。
- 运行/调试要求：先以最小命令跑通 CLI 入口；随后围绕一次 prompt 请求追踪核心路径；涉及真实模型调用、登录、网络或权限时记录替代验证方案。
- Mini demo 方向：实现一个最小 Agent CLI，保留“命令入口 -> 会话/上下文 -> 工具选择 -> 权限/沙箱检查 -> 结果回传”的核心结构。

## 角色边界

- daedalus 应该做：拆解学习路径、生成行动指南、协助排障、验收阶段产物。
- 用户必须亲自做：确认目标、运行关键命令、观察结果、回答核心问题、形成自己的笔记。
- daedalus 可以协助但不能代替：源码拉取、环境启动、问题回答、架构总结、demo 设计。
